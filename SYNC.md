# Sync Log — Zealot of Harpurhey

This is the shared handoff log between everyone working on this repo: Codex, Claude Code, and Daniel. Treat it like a standup. It exists because Daniel alternates between Codex and Claude depending on credits, and the two tools have no way to see each other's reasoning — the repo (commits, code, docs, this file) is the only channel between them.

## How to use this file

1. **Starting a session:** read the last 2–3 entries below, then verify against reality with `git log --oneline -20` and `git status`. This file records intent and context; git is ground truth. If git shows commits or working-tree changes not mentioned in the log, that's undocumented work from the other engineer — inspect it before building on it or changing it.
2. **Ending a session, or before switching tools:** append a new entry at the top using the template below. Keep it short — bullet points, not prose. This is a standup log, not documentation; real documentation belongs in `docs/`.
3. **Open questions:** if you need an answer from "the other engineer" (human or AI) before proceeding, write it under `Open questions` in your entry. Whoever picks up next answers it in their own entry rather than editing yours.
4. If this file grows past ~15 entries, move the older ones to `docs/SYNC_ARCHIVE.md` and keep this file to the recent handful. Keep entries short: findings and measurements that will matter later belong in `docs/`, with the entry linking to them. Don't read the archive at session start; grep it only when you need the history behind something.

### Entry template

```
## YYYY-MM-DD — <Codex | Claude | Daniel>
**HEAD at session start:** `<hash>` (<subject line>)
**Did:** what changed and why, in a few bullets.
**Left uncommitted (if any):** what's still in the working tree and why.
**Flagged:** anything that looked wrong, stale, or worth someone else's attention.
**Next:** the recommended next step, if any.
**Open questions:** anything you need the other engineer or Daniel to weigh in on.
```

---
## 2026-09-24 — Claude (Stage 2 of the realism pass: the grade) — branch `realism-pass`
**HEAD at session start:** `21c2dd1` (Merge main into realism-pass), on `realism-pass`.
**Did:** Implemented Stage 2 of `docs/REALISM_PASS_PLAN.md` — the code is done; the look is not yet approved (see "Flagged"). All in `src/rendering/visualStyle.ts` and `src/rendering/createPostProcessing.ts`:
- **Black lift**: new `blackLift: 0.045` uniform, `color * (1 - blackLift) + blackLift` after contrast/before split-tone, so true black maps to ~4.5% display luminance instead of crushing flat. No exposure/ambient change.
- **Split-tone**: `saturation` 1.12 → 1.0; new luminance-weighted push toward a teal shadow tint (`#8fb4c9`) and amber highlight tint (`#dcb98c`), strength 0.16, blended between luminance 0.12 and 0.72 (post-lift, so the edit reads against the displayed image).
- **Bloom retune**: radius 0.32 → 0.22, threshold 0.88 → 0.92, strength 0.3/0.34 → 0.24/0.26 (medium/high) — a tighter halo on genuinely bright sources rather than a haze over midtones.
- **Live tuning API**: `GradeParameters` interface with every value independently get/settable (`getGradeParameters`/`setGradeParameters` on the post-processing pipeline), wired into `window.zealot.grade.getParameters()`/`.set({...})` in `main.ts` (dev only) — the same shape `window.zealot.atmosphere` already uses, rather than inventing a `?grade=` URL DSL.
- Reconciled `docs/VISUAL_LANGUAGE.md`'s "Central configuration" and "Post-processing limits" sections with the new values and the `zealot.grade` dev tool.
**Validation:** `npx tsc --noEmit` clean, `npm test` 43/43, `npm run build` clean. Ran the actual app (Chromium/SwiftShader, no GPU here) at `dreams-target`/`dreams-angle` — visibly less crushed shadow detail (the dark shop wall and road hold more dark blue-grey information instead of near-black) and a visibly tighter bloom halo around the Dreams tubes, no console/shader errors. Screenshots: `renders/realism-pass/02-grade/`.
**Left uncommitted (if any):** None — commit follows this entry.
**Flagged:**
- **This is the important one: the numbers are a reasoned first pass, not an approved grade.** The plan's actual "done when" is Daniel signing off one grade on `dreams-target` next to the reference — that hasn't happened. Use `zealot.grade.set({...})` in the browser console to tune live, then report the values back so they get baked into `VISUAL_STYLE.render`/`VISUAL_STYLE.bloom` and this stage gets marked fully done, not just code-complete.
- No real fps/GPU measurement, same SwiftShader limitation as Stage 1. Bloom's `strength`/`radius`/`threshold` changes should be near-free (same pass, different constants) but worth a real fps check alongside the tuning pass.
**Next:** Daniel: `git fetch && git checkout realism-pass && git merge origin/realism-pass`, run locally, open the console and try `zealot.grade.set({...})` with different `blackLift`/`splitToneStrength`/`bloomThreshold` values against the Dreams reference. Report back what looks right (or say "close enough, ship it") and I'll bake it in and move to Stage 3.
**Open questions:** None new.

---
## 2026-09-24 — Claude (merged main into realism-pass) — branch `realism-pass`
**HEAD at session start:** `1572a41` (Stage 1 of the realism pass), on `realism-pass`.
**Did:** Daniel reported "a lot of bugs" testing `realism-pass` locally and asked whether it had main's current changes. It didn't: `realism-pass` had been branched off an earlier documentation-only branch (`claude/vigilant-fermi-i4wvhe`), which itself diverged from `main` at `c5cb142` — before four commits landed on `main`: the asset-optimisation pass (KTX2/Meshopt, 128.8 MB → 51.7 MB), the finite-color NaN black-screen guard, a batch of concurrent Codex work (Sterling bike materials, GPU scene preparation, camera fixes, Greek Gyros/bus shelter textures), and the Off-Licence removal. That's almost certainly what Daniel was seeing as "bugs" — an old `loadModel`/texture pipeline missing several since-landed fixes, not a Stage 1 regression.
- `git merge origin/main` into `realism-pass`. Two conflicts: `src/rendering/createPostProcessing.ts` (both branches added a pass near the same spot — main's `finiteColorShader` pass right after `RenderPass`, this branch's `SMAAPass` before `OutputPass`) resolved **automatically** by git with the correct order (RenderPass → finite-color guard → bloom → SMAA → OutputPass → grade); `SYNC.md` (both branches appended entries) resolved by concatenating both logs in date order, newest first.
**Validation:** `npx tsc --noEmit` clean, `npm test` 43/43 (up from 17/17 — main's merged work added tests), `npm run build` clean.
**Left uncommitted (if any):** None — commit follows this entry.
**Flagged:** `realism-pass` and `main` are now identical except for Stage 1's five changes (`visualStyle.ts`, `createPostProcessing.ts`) plus the realism-pass documentation (`docs/VISUAL_REALISM_ROADMAP.md`, `docs/REALISM_PASS_PLAN.md`, the `AGENTS.md`/`VISUAL_LANGUAGE.md` reconciliation, `renders/visual-gap-2026-09-24/`, `renders/realism-pass/`) — confirm with `git diff main realism-pass -- src` if in doubt. Going forward, keep `realism-pass` merging from `main` regularly (or the other tool should branch its own work from `main` too) so this doesn't recur.
**Next:** Daniel: `git fetch && git checkout realism-pass && git merge origin/realism-pass` (or reset if no local commits), rebuild, and re-check whether the bugs are gone.
**Open questions:** None new.

---
## 2026-09-24 — Claude (Stage 1 of the realism pass: retro settings off) — branch `realism-pass`
**HEAD at session start:** `45304f8` (Add a staged, step-by-step plan for the realism pass), then branched to `realism-pass` per Daniel's request (a dedicated branch for the realism-pass work, pushed and pullable to his laptop).
**Did:** Implemented Stage 1 of `docs/REALISM_PASS_PLAN.md`. All in `src/rendering/visualStyle.ts` and `src/rendering/createPostProcessing.ts`:
- `geometry.facetedLighting` → `false` (flat shading off everywhere it's read: `worldMaterials.ts`, road/pavement/park/decal/graffiti surfaces).
- `colorQuantizationLevels` 32 → 255 (a no-op at 8-bit output, so it stops visibly posterising dark gradients); `ditherStrength` 0.003 → `1/255`, just enough to break up banding.
- `applyTextureProfile`'s `RETRO_GRAPHIC` branch: nearest/nearest-mip → linear/linear-mipmap-linear. Checked all 5 call sites (`worldGraphics.ts` weathered sign/graffiti/notice/sticker cluster, `createWorld.ts` road annotation) — all are canvas-drawn signage, kept as `RETRO_GRAPHIC` for its lower anisotropy, just no longer pixellated.
- Added `SMAAPass`, gated by a new `QualityProfile.smaaEnabled` (on at MEDIUM/HIGH, off at LOW). **Corrected the plan's stated placement**: SMAA has to run before `OutputPass` (it operates in linear-sRGB; `OutputPass` does the tone-map/colour-space conversion), not "after the grade" as the plan said — order is now RenderPass → Bloom → SMAA → OutputPass → grade.
- Reconciled docs: `docs/VISUAL_LANGUAGE.md` (title, "Central principle", "Central configuration" values, "Texture profiles", "Post-processing limits" — all previously described/configured the old retro-console target). Also fixed `AGENTS.md`'s caveat, which on inspection had it backwards: `ART_DIRECTION.md`'s "Core principles" already states the current non-retro direction and was never stale; `VISUAL_LANGUAGE.md` was the actually-stale document.
- Marked Stage 1 done, item by item, in `docs/REALISM_PASS_PLAN.md`.
**Validation:** `npx tsc --noEmit` clean, `npm test` 17/17, `npm run build` clean (same pre-existing >500kB chunk advisory as before, unrelated). Visually confirmed via Chromium/SwiftShader screenshots at `dreams-target`/`dreams-angle`/`park-to-dreams` (`renders/realism-pass/01-retro-settings-off/`, compare against the Stage 0 baseline in `renders/visual-gap-2026-09-24/`): smoother sky gradient, smoother sign text, no console/shader errors.
**Left uncommitted (if any):** None — commit follows this entry.
**Flagged:**
- **No real fps/GPU measurement yet.** This automation environment is CPU/SwiftShader-only; the Stage 1 "done when" fps budget in the plan is not actually confirmed. Daniel should run the app on real hardware and compare against the Stage 0.3 baseline (not yet captured either — Stage 0 was skipped to get to Stage 1 faster; worth doing retroactively).
- Step 2 of Stage 1 (checking for lumpy GLB normals now that flat shading is off) needs a real GPU pass to judge properly — not reliably checkable on SwiftShader.
- Blender is still 3.0.0 per `docs/TECHNICAL.md` (Stage 0.1, not done).
**Next:** Daniel: `git fetch && git checkout realism-pass`, run locally, sanity-check fps and look for lumpy geometry anywhere in the map (not just the three dev views captured here). Then Stage 2 (the grade) or Stage 0 cleanup (Blender upgrade, baseline capture, landing the KTX2 work), whichever Daniel wants first.
**Open questions:** None new. Still open from the prior entry: free vs paid character-pipeline route (Stages 7–8).

---
## 2026-09-24 — Claude (step-by-step realism pass plan)
**HEAD at session start:** `1028eb1` (Add visual realism roadmap against the Dreams night reference)
**Did:** Wrote `docs/REALISM_PASS_PLAN.md`, the executable companion to `VISUAL_REALISM_ROADMAP.md`. It has 10 stages, each with owner, files and a done-when check:
- 0: Blender upgrade, land KTX2, baseline
- 1: retro settings off
- 2: grade
- 3: texture standard and materials
- 4: env map, puddle mask, light streaks
- 5: **baked lightmaps on Dreams, the proof gate**
- 6: player/bike shadows
- 7: body and animation
- 8: denim and kit
- 9: rollout
No code changes.
**Left uncommitted (if any):** None.
**Flagged:**
- Blender on Daniel's Mac is still 3.0.0 per `TECHNICAL.md`. Upgrade first.
- The KTX2/Meshopt work mentioned in the Codex entries is not in the repo.
- `geometry.shadowsEnabled` is read by nothing.
**Next:** Stage 0.3 baseline on real hardware, then Stage 1 (a code-only session can do it).
**Open questions:** Daniel: free (MPFB2 + Blender cloth) or paid (Character Creator 4 + Marvelous Designer) character route?

---
## 2026-09-24 — Claude (visual-gap consultation: "Lego" → the Dreams night reference)
**HEAD at session start:** `c5cb142` (Archive older SYNC.md entries to docs/SYNC_ARCHIVE.md)
**Did:** Analysis only, no code changes. Full report: `docs/VISUAL_REALISM_ROADMAP.md`. Target, plus fresh current-state captures: `renders/visual-gap-2026-09-24/`.
**Simplified report:**
- The target image is an AI paint-over of our own V3 Dreams screenshot. Layout, scale and camera are already right; the gap is surfaces, light and density.
- The main cause of the "Lego" look is **leftover Dreamcast-era config**, not missing tech:
  - `facetedLighting: true` puts flat shading on all world materials.
  - `scene.environment` is never set, so materials have nothing to reflect.
  - Shadows are off entirely: `renderer.shadowMap` is never enabled, so every `castShadow` does nothing.
  - The grade posterises to 32 colour levels.
  - World textures are 128–512 px, with few normal maps.
  - Hero geometry is code-built boxes in `createWorld.ts`.
- **Plan (cheap to expensive):**
  - A: turn the retro flags off and add SMAA.
  - B: a night HDR environment map, a wet-road PBR material with a puddle mask, and per-light stretched reflection billboards.
  - C (**the one structural change**): bake lightmaps and AO in Blender per hero building (second UV set, Cycles bake), which gives realistic light at near-zero runtime cost.
  - D: one soft shadow for the player and bike only.
  - E–G: 1–2K PBR plus KTX2 textures, bevels/trims/decals, rain, halos, a grade retune.
  - H: land KTX2/Meshopt, instancing, LOD and the pre-warm fix, to pay for it.
- **Stay on Three.js.** Leave WebGPU until the art is locked.
- **Character:** replace both the primitive mesh and the sine-wave procedural animation. Use MPFB2 (free) or Character Creator 4 for the body, Marvelous/Blender cloth plus retopo for the denim, Mixamo or phone-mocap clips via `AnimationMixer`. Budget 20–35k triangles and 2K KTX2 textures.
**Left uncommitted (if any):** None.
**Flagged:** `docs/ART_DIRECTION.md` "Core principles" is updated, but `visualStyle.ts` still enforces the retro look. Reconcile them when Phase A lands. The dev server was started with `npm run assets:prepare` (production manifest) for screenshots only.
**Next:** Phase A, then a Dreams-only vertical slice of B and C, compared side by side with the reference.
**Open questions:** Daniel: is the reference's blocky "mosaic" grain wanted as a deliberate stylistic layer, or an AI artefact to ignore? Is a paid tool budget available (Character Creator 4 / Marvelous Designer)?

---
## 2026-09-24 — Claude (removed the Off-Licence)
**HEAD at session start:** `b7be7eb` (Land the concurrent Codex work...)
**Did:** Daniel asked for the Off-Licence to be removed from the game.
- Dropped the `off-licence` placeholder from `WORLD_LOCATIONS` (`worldLayout.ts`), its sign style and its fascia reflection patch (`createWorld.ts`). Nothing else referenced it (no missions, views or tests).
- The Off-Licence was hiding Spice Cabin's east party wall, which has no exterior face. New `addSpiceCabinPartyWall` closes it with a 0.3 m `brick-soot-overhaul` box at X = 17 to 17.3 (full 10 m depth, 7.65 m height, with collision). Docs updated: `docs/WORLD_LAYOUT.md`, `docs/assets/spice-cabin.md`.
**Verified:** `tsc` clean, 43/43 tests. In the running game there is no Off-Licence object and the wall is in place at (17.15, 49); from the east the gable is closed.
**Flagged:** The new wall has no light on it, so at night it reads as a near-black slab from the east, like other unlit placeholder sides. A proper brick party face, or something built on the empty plot (X 17 to 28, Z 44 to 54), would fix that.
**Left uncommitted:** `.claude/launch.json` and `.playwright-mcp/` (not mine).
**Open questions:** Should the empty plot stay empty, or should something go there?

---
## 2026-09-24 — Claude (asset optimisation: 128.8 MB down to 51.7 MB, texture memory about 2 GB down to about 120 MB)
**HEAD at session start:** `c84ad52`; pushed as `be2f8be`.
**Did:** Daniel asked for the large files causing performance problems to be optimised. Measured first: the production manifest decoded to **about 2,040 MB of texture memory in the models** (roughly 100 maps at 2048², 22 MB each once decoded) plus 183 MB of loose textures, and forcing every off-screen mesh to render once took **21 s** in the in-app browser. That is the first-use stall the previous entry chased, so this is upstream of Codex's `prepareScene` work: their pre-warm makes loading time equal upload time, and this makes the upload cheap.
- New build stage `scripts/optimizeRuntimeAssets.mjs` (+ `scripts/lib/`), called from `prepareRuntimeAssets.mjs`. **`public/` is untouched**; only the copy in `.runtime-public/` is optimised. Full description, tuning knobs and rationale: `docs/ASSET_PIPELINE.md`.
- Textures: resized to about 400 texels/m (measured per texture from UV area / world area; the authored maps had 0.3-1 mm texels against a 2-4 mm screen pixel), **63 of 106 normal maps were perfectly flat and 25 of 115 ORM maps constant, so those are dropped**, the rest become KTX2 (ETC1S colour/ORM, UASTC normals/alpha).
- Geometry: lossless Meshopt everywhere (skinned included); 14-bit quantised positions only for Real Camera and the Coral shop (`config/asset-optimization.json`). Every GLB is decoded again after encoding and must keep every mesh's world bounds within 4 mm or the build fails.
- Loose PNG/JPEG recompressed in place (alpha PNGs untouched: re-encoding zeroes colour under transparent pixels and filtering then bleeds dark halos). Loading screen now ships as `zealot-loading-screen.webp` (749 KB vs 3.3 MB PNG); the three unused title PNGs (7.5 MB) no longer ship. `public/_headers` adds cache headers (live host was `max-age=0, must-revalidate` on every file, about 140 revalidation round trips per visit).
- Runtime: `src/world/gltfLoader.ts` (single `GLTFLoader` with KTX2 + Meshopt) and `src/world/dequantizeGeometry.ts`. `loadModel.ts` is the only caller change.
**Numbers (shipped bytes, production manifest):** total 128.8 -> 51.7 MB; models 98.4 -> 40.4; loose textures 19.6 -> 10.0; title art 10.8 -> 0.75. Biggest models: Real Camera 19.2 -> 5.7, Vinyl Exchange 14.9 -> 1.5, Coral shop 4.6 -> 0.5, The Hive 4.6 -> 1.4, Greek Gyros 3.5 -> 0.2, player 4.8 -> 1.6. Estimated texture memory in models 2,040 -> 117 MB.
**Verified:** 43/43 tests, `tsc` clean, `npm run build` and `wrangler deploy --dry-run` clean, and the commit alone passes in a clean worktree. In the running game against an unoptimised build of the same source: no console errors, same merged geometry count (693 vs 708 GPU geometries), 255 textures arrive GPU-compressed (ASTC), and screenshot pixel diffs of 0.06% (Real Camera) and 0.24% (Spice Cabin) with lettering, timber and quantised stonework visibly identical. One real bug found and fixed this way: my dequantiser skipped `InterleavedBufferAttribute`, which is what Meshopt pads quantised attributes into, so 590 meshes silently stopped merging (1,298 geometries) until fixed.
**Flagged:**
- **Not deployed.** Nothing is live until Daniel runs `npm run build` then `wrangler deploy` **from a machine with `basisu`** (`brew install basis_universal`, already installed on this Mac). Without it the optimiser falls back to resized WebP (what CI does) and says so at the top of the log: still a big win, but not GPU-compressed.
- **First build on a fresh clone takes 20-25 minutes** (texture encoding); results are cached by content hash in `.cache/asset-optimizer/` (271 MB, gitignored), after which an unchanged tree takes about 20 s. `ZEALOT_OPTIMIZE_ASSETS=0` skips it.
- **Some files got bigger on disk**: the bus shelter (2.4 -> 4.4 MB) and Spice Cabin (7.2 -> 6.0, was 8.8 before capping normals) were already compact WebP; KTX2 trades a little download for a 10-20x cut in GPU memory and no image decode. If softness is ever visible up close, raise `targetTexelsPerMetre` in `scripts/lib/glbOptimizer.mjs` (each doubling quadruples that texture's memory).
- **Timing evidence is weak.** The in-app browser reported load-to-warm 16.4 s (unoptimised) vs 8.5 s (optimised) on localhost, but a headless Chromium from another session was using about 360% CPU at times, and the pane later went `visibility: hidden` (no animation frames), so **I never got a clean walking frame-time comparison for the optimised build**. Byte, memory and count figures are deterministic; the seconds are indicative only. Worth one clean pass on Daniel's real Chrome.
- Loose textures still decode to about 183 MB (KTX2 for them needs `loadSurfaceTexture` and `worldMaterials` to use `KTX2Loader`; not done). Audio (102 MB, development-only) is untouched. Scene triangle count is unchanged (about 777k; Real Camera alone is about 245k), so draw-call/triangle reduction is still open.
- Side effect on other sessions: `npm run dev` now runs the optimiser via `predev`, and I re-ran `assets:prepare:dev` (twice) under a live dev server on 5173. `ZEALOT_RUNTIME_OUTPUT=<dir>` builds and serves an alternative asset set without touching `.runtime-public/` (I used it for A/B).
**Left uncommitted:** nothing of mine. Other sessions' work (Greek Gyros, bus shelter, Sterling bike, Cass/camera fixes, `prepareScene`) is untouched.
**Next:** (1) Daniel: deploy and play on the live embed, then report whether the stutter/long loads are gone. (2) One clean frame-time pass on a real browser. (3) If wanted: KTX2 for loose textures, then triangle/draw-call reduction (Real Camera first).
**Open questions:** Is 2.5 mm/texel the right softness trade-off, or should hero surfaces near the delivery route keep more?

---
## 2026-09-24 — Codex (exported Sterling bike materials into the game)
**HEAD at session start:** `c84ad52` (Record the focus fix and flicker investigation in SYNC.md)
**Did:** Found that the September 23 Sterling rear-cover/material pass was already the latest authored `.blend` and GLBs, but those GLBs embedded zero images: Blender Noise/Map Range/Bump nodes were not carried into Three.js, leaving key parts white. Updated `exportSterlingBikeBlockout.py` to UV unwrap and bake the authored procedural finishes into PNG atlases and embedded GLB textures; regenerated both bike/dock GLBs. Removed the stale runtime blockout roughness override and bumped the asset URLs. Added a GLB regression check for paint maps, UVs and articulated nodes, and updated the asset document.
**Validation:** Authored GLBs now contain 25 bike and 10 dock images; the optimized development assets retain 16 and 8 useful textures respectively. `npm run assets:prepare:dev`, TypeScript, the 42-test suite, targeted GLB test, and `git diff --check` passed. In the live `?view=sterling-south` browser view, the bikes now show yellow frames, teal round rear covers, dark baskets/tyres and dock colors; no browser warnings/errors. The development server remains running at `127.0.0.1:5173`.
**Left uncommitted (if any):** This session's export script, baked texture directory, two GLBs, `docs/assets/sterling-bike.md`, targeted test, `src/world/createWorld.ts` Sterling loader edits, and this log. Other working-tree work from concurrent sessions was preserved. No commit or deployment.
**Flagged:** The optimized asset pipeline drops very fine normal maps as imperceptible but retains color and roughness maps. Branding decals/dirt and final non-blockout naming remain deferred as documented. An initial test run overlapped another session's optimizer-test edit; the clean rerun passed 42/42.
**Next:** Daniel can review the textured bikes in localhost; later decal/branding work should start from the authored Blender material source and this export path.
**Open questions:** None.

---
## 2026-09-24 — Codex (opened local game for testing)
**HEAD at session start:** `c84ad52` (Record the focus fix and flicker investigation in SYNC.md)
**Did:** Started `npm run dev -- --host 127.0.0.1`; development asset preparation completed and Vite served the game at `http://127.0.0.1:5173/`. Opened it in the Codex browser and confirmed the loading screen cleared and the park spawn rendered visibly.
**Left uncommitted (if any):** This log entry only; all other working-tree changes predated this session. The Vite server was left running for Daniel to test.
**Flagged:** None from this launch.
**Next:** Daniel can play the local game in the open browser tab.
**Open questions:** None.

---
## 2026-09-24 — Codex (fixed local black screens, camera wall penetration and first-use stalls)
**HEAD at session start:** `c84ad52` (Record the focus fix and flicker investigation in SYNC.md)
**Did / findings:**
- Reproduced completely black views on localhost while the simulation, camera, and draw calls continued. No GL errors or context loss were required. Instrumented the actual HDR render targets with half-float pixel readbacks rather than treating this as merely low FPS.
- **Confirmed black-screen mechanism:** one invalid RGB pixel (3 NaN components) in the scene target spreads through UnrealBloomPass's successive blur mips. In the captured 531×732 frame it contaminated all 1,166,076 RGB components; the tone-mapped target had NaN alpha, and the final canvas was RGBA zero. This explains the view-dependent flicker/full black screen. The earlier statement that no rendering/lighting bug remained was incorrect.
- A raycast through offending pixels identifies Sterling bike `SB_SteeringRoot MAT_SB_Metal` geometry. The GLB exports `KHR_materials_anisotropy` (metal 0.35; rims 0.5), but `mergeSterlingStaticParts` deletes every attribute except position/normal, including the UVs/tangents needed for anisotropic shading. Some original metal primitives also lack UVs. The source incompatibility is fixed below.
- Added `finiteColorShader.ts` immediately after RenderPass and before bloom: non-finite channels are repaired locally before blur can spread them. Already measured: raw scene still contained 6 NaN RGB components at the reproduced view; **all downstream buffers had 0 NaNs and the final canvas was opaque/nonblack**. No lighting/art-direction changes.
- **Confirmed separate camera bug:** the previous 0.6 m minimum-boom fix overrides a valid wall collision. A regression test with the player 0.38 m from a wall put the old lens 0.2195 m inside the wall. Removed that lower bound, retained finite orbit-facing orientation at collapsed separation, and added the brief's close-figure hide hysteresis (1.7/1.95 m; contact shadow retained). Both camera tests failed before and pass after; 720 orbit steps keep the lens outside the wall.
- **Confirmed loading flaw:** installed Three.js `compileAsync` traverses the whole scene, regardless of frustum. The previous 8-direction sweep recompiles the same materials and does not upload geometry/textures. Replaced it with one HDR-matched compilation plus actual off-screen rendering in 64-object batches, preserving/restoring the live camera, layers, culling and render target before every yield. Unit tests cover off-screen descendants and restoration after failures.
- A 30-second movement/orbit run after GPU preparation (before adding the NaN guard) averaged 18.31 ms (~55 FPS), median 16.7 ms, p95 33.2 ms, max 183.4 ms, no >250 ms stalls, no GL errors. Programs remained 75; geometry 701→709 and textures 462→466 (small residual first-use allocations). It **still went black**, which led to the NaN diagnosis above; do not mistake the timing pass for a complete visual fix.
- **Source fix completed:** extracted the existing Sterling batching function to `mergeSterlingStaticParts.ts` for regression testing; it now groups by compatible vertex layout, retains UV/tangent attributes, preserves articulation, and reverses all per-vertex attributes plus tangent handedness on mirrored transforms. Original anisotropic parts without any tangent frame receive a cloned isotropic material; valid shared materials are unchanged. Regression verifies UV preservation, pivot ownership, mirrored tangents and isolated fallback.
- After the source fix, the reproduced east-facing street view measured **0 NaN/Inf components in the raw scene and every downstream buffer**, and the final canvas was opaque with visible scene pixels. This is stronger evidence than merely observing no console errors. `tests/browser/finite-color.html` is a durable GPU regression for deliberately invalid half-float values; it passed on the actual browser/GPU, with exact expected readbacks and GL error 0.
- Full Node suite passed **33/33**, including the concurrently added asset optimizer tests; TypeScript passed. Wider 1280×720-class viewport run (1152×647 drawing buffer) while the Node/typecheck processes were active: average 32.79 ms, median 33.2 ms, p95 66.6 ms, maximum 399 ms, 3 >250 ms gaps; no GL errors, no black final view, programs 77 unchanged, textures 466 unchanged, geometries 724→725. This is not directly comparable to the earlier narrow viewport or proof of universal 60 FPS; the quieter follow-up result is recorded below.
- Follow-up 30-second walking/orbit run after this session's Node/typecheck processes completed: average **23.14 ms (~43 FPS)**, median **16.7 ms**, p95 **34.4 ms**, maximum **183.3 ms**, **0 >250 ms stalls and 0 GL errors**. Programs stayed at 77; geometry 725→730 and textures 466→470. Both wide-viewport runs ended with a visible scene. These are route/viewport-specific measurements, not a claim of locked 60 FPS throughout the map.
- Validation: **33/33 Node tests**, TypeScript, runtime asset validation (37 entries), rights check, browser finite-HDR GPU test, and Vite production code build all passed. Build output was directed to `/private/tmp/zealot-rendering-check` without running the concurrently changing asset-optimization/prebuild pipeline or replacing its `.runtime-public` output; this validates code bundling, not the other session's finished release pipeline. Vite retains its large-chunk advisory (about 898 kB including that session's decoder changes).
- Final normal-page smoke test (`http://localhost:5173/`): loading plate completed, normal gameplay rendered visibly at the park spawn, and browser warning/error logs were empty.
- Updated `docs/PERFORMANCE.md` with the measured NaN mechanism and preparation policy, and `docs/TECHNICAL.md` with hard camera clearance and character-hide behaviour. Removed all temporary diagnostic/baseline HTML/JS and `src/main-baseline.ts`; retained only the purposeful browser GPU regression under `tests/browser/`.
**Left uncommitted (if any):** This session: `src/main.ts`, camera/player changes, `createPostProcessing.ts`, new `finiteColorShader.ts`/`prepareScene.ts`, extraction/fix of Sterling batching (`createWorld.ts`, `mergeStaticModelMeshes.ts`, new `mergeSterlingStaticParts.ts`), three Node regression files, `tests/browser/finite-color.html`, the two technical docs, and this log. No commit, push or deployment performed. Pre-existing bus-shelter/Greek Gyros work and concurrent optimizer/loader work are preserved.
**Flagged:** Another session started writing asset-compression/KTX2/Meshopt work during this investigation (`package*.json`, `.gitignore`, `src/world/loadModel.ts`, new `gltfLoader.ts`, optimizer scripts/tests). These are **not this session's changes**, and will be preserved. Independent validation is complete as recorded above; the other session's evolving compression/prebuild pipeline is outside this validation. Prior `GL_INVALID_FRAMEBUFFER_OPERATION` reports have not been reproduced here; the confirmed NaN failure occurs with `gl.getError() === 0`.
**Next:** Daniel can reload localhost and play normally. Coordinate committing/deploying this fix with the concurrent asset-optimization work. Remaining general performance work is scene submission/visibility/LOD in heavy views; GPU prewarming does not remove all draw-call costs or guarantee universal 60 FPS.
**Open questions:** None blocking.

---
## 2026-09-24 — Claude (fixed "can't walk" in the embed; found but only partly fixed the flicker/stutter)
**HEAD at session start:** `24da09e` (Record the live-embed diagnosis and custom-domain fix in SYNC.md)
**Did:**
- Daniel reported he couldn't move at all on the live `danieloye.com` embed. Root-caused: clicking the game canvas never reliably handed this frame real keyboard focus — `document.activeElement` on the parent stayed `<body>` after the click and the parent never fired `blur`, confirmed reproducibly in two browsers, while a plain control iframe on an unrelated test page focused correctly under the identical click. First fix (canvas `pointerdown` → `window.focus()`, commit `0196abc`) didn't engage in practice: the loading plate covers the whole viewport with no `pointer-events: none`, so an eager player's *first* click — exactly when someone tries to start playing — lands on the plate, not the canvas. Real fix: claim focus on the very first `pointerdown` anywhere on the page, in `index.html` itself, before the loading plate, before the module script (`773920d`). Verified locally by embedding the dev server in a genuine cross-origin-style iframe test harness: before, clicking never moved focus off the parent and no `blur` ever fired; after, the parent fires `blur` and `activeElement` becomes the iframe — first time that's happened in extensive repeated testing. Confirmed live after Daniel deployed: `activeElement` is now `IFRAME` on the real page, and Daniel confirmed he can walk around.
- Daniel then reported flickering, worse now that movement works. Idle/stationary testing (screenshots, 60fps `requestAnimationFrame` timing) never showed anything. The actual signal only showed up under objective frame-timing instrumentation *while walking*: on production, average frame delta during a walk was 35.77ms (vs. steady 16.6ms idle), with **gaps up to 1132ms and 1916ms** on two separate walks — real freezes, not a rendering/lighting logic bug. Ruled out the `LocalLightRegistry` light-budget churn I'd originally suspected (a prior session's own design, meant to solve exactly this kind of freeze) — instrumented it directly via the dev build's `window.zealot.scene` and confirmed transitions are sparse, smooth, and as designed (a handful of fades over 20+ seconds, not per-frame flapping). Instrumented `renderer.info` instead: `geometries`/`textures` counts grow substantially during a walk (e.g. 267→327 geometries, 183→207 textures over 12s) — Three.js uploads a mesh's GPU buffers and compiles its shader lazily, on first actual render, not at GLB-parse time, so turning toward anything not swept by the existing one-shot `compileAsync(scene, camera)` at spawn pays that cost live, as a stall.
- Extended the existing precompile step (`773920d`) to sweep the compile camera through a full rotation at spawn instead of one fixed frustum, during the loading screen's existing hold. **Measured this does not fix it**: re-ran the same before/after `renderer.info` + frame-timing walk and got the same growth, same gap pattern. Root cause: a rotation-only sweep from one point still can't warm content that's occluded or simply out of range from spawn — the case that actually matters is walking far enough, or around enough corners, to see something for the first time from anywhere. Kept the change anyway since it's a genuine (if partial) improvement at near-zero cost, not a regression.
**Left uncommitted (if any):** None — both commits (`0196abc`→`773920d` focus fix path, `f112d16` shader-sweep) pushed. Scratch test files (`iframe-focus-test.html`, `lazy-test.html`, `focus-fix-test.html`, `.claude/launch.json`'s `iframe-focus-test` server entry) live only in this session's scratchpad, not this repo.
**Flagged:** The stutter is real, reproducible, and gets *worse* the more you walk (a second identical test walk had a 1916ms freeze vs. the first's 1132ms) — consistent with exploring progressively more never-before-rendered geometry, not a leak that would show up even standing still. A real fix needs either (a) a multi-position pre-warm sweep across the playable area (`WORLD_BOUNDS` in `collision.ts`) before the title card lifts — real, possibly substantial added loading time, a UX tradeoff Daniel should weigh in on — or (b) a different architecture entirely (streaming/priority loading, disposing what's off-screen). Also: dev build (`localhost:5173`) and production (`game.danieloye.com`) disagree on world layout at the same spawn/walk script (dev landed at a different, unlit storefront) — untouched, not investigated, just noting the two aren't identical test environments.
**Next:** Daniel to decide: accept the current partial mitigation, or greenlight a proper multi-position shader/asset pre-warm sweep (adds loading time) as the next piece of work.
**Open questions:** How much extra loading time is acceptable in exchange for eliminating the mid-walk stutter? Is a full pre-warm sweep across the whole map the right approach, or does this call for a bigger architecture change (streaming/LOD/disposal) instead?

---
## 2026-09-24 — Claude (found and fixed the real cause of the live embed being unplayable)
**HEAD at session start:** `2ffdcaa` (Fix black screen when the camera boom collapses onto its pivot near walls)
**Did:**
- Daniel asked to check the live embed at `danieloye.com/zealot-of-harpurhey/` via Chrome. It never rendered anything — solid black indefinitely, confirmed on repeated fresh loads waited out past 30s, while the direct `workers.dev` URL always loaded fine within a few seconds.
- Root-caused it: cross-origin requests (fetch and, critically, the iframe's own navigation) to the `*.workers.dev` URL returned 503, reproduced identically from three unrelated origins (`danieloye.com`, `example.com`, raw `fetch()`), while same-origin top-level navigation and even `curl` always succeeded. Ruled out Cloudflare Access (confirmed disabled via API) and X-Frame-Options/CSP (confirmed absent via `curl -D -`). Landed on: the Worker was only ever deployed on the shared `workers.dev` subdomain, which Cloudflare's own docs describe as a restricted free/hobby tier — this was already flagged as an unfinished step in the 2026-09-23 "Harperhey→Harpurhey" entry below and never done.
- Fix: attached a real Cloudflare Custom Domain, `game.danieloye.com`, to this Worker (Daniel ran the actual `wrangler deploy` himself after the MCP tool and a local `wrangler deploy` both correctly hit a DNS/cert-change permission gate). Verified the fix directly — not just assumed — by embedding `game.danieloye.com` in a real cross-origin iframe on a third-party page and watching it render.
- Found the `danieloye.com` site's own repo (**not this one** — it's `~/GitHub/WXVERI2`, GitHub `danieloyegade/wxvei`, Worker name `daniel-oyegade`) and updated its iframe `src` in `src/pages/zealot-of-harpurhey.astro` from the old `workers.dev` URL to `https://game.danieloye.com/`. Built locally to confirm, committed, pushed to `main`, watched that repo's own CI deploy it. Confirmed live: `danieloye.com/zealot-of-harpurhey/` now actually renders the title screen.
- Committed the `game.danieloye.com` custom-domain route into this repo's `wrangler.jsonc` (it had only been attached live, never declared in config — a future deploy from a clean checkout would otherwise have silently dropped it).
**Left uncommitted (if any):** None from this task — everything above is committed and pushed on both repos.
**Flagged:** The old `workers.dev` URL still works standalone and is still the Worker's default route (Custom Domains add to, not replace, the `workers.dev` route) — not a problem, just noting both URLs are live. Also: automated browser tooling (both Claude's built-in browser pane and the Chrome extension) reports `document.hidden = true` for tabs it drives, which makes the game's own (correct) `InputController.resetTransientInput()` discard held-key state — so held-key movement/collision/camera-near-wall testing isn't possible through this automation. Not a game bug; a prior session (2026-09-23) hit and documented the same thing. A real interactive playtest is still owed on this "boom collapse near walls" fix from earlier today, since it was only verified via code + unit tests, never actually played.
**Next:** Daniel to do a real playthrough of the live `danieloye.com` embed (movement, camera near shopfront walls, bike riding, delivery loop) — first proper interactive check since the embed actually started working. Otherwise the still-unresolved branch consolidation (`claude/engineer-communication-workflow-uex7id`, `claude/local-cloud-workflow-h6ivy0`) from the 2026-09-23 entries further down remains open and untouched by this session.
**Open questions:** None.

---
## 2026-09-23 — Claude (diagnosed the live "black screen when turning" report)
**HEAD at session start:** `6692d64` (Fix the Sterling Bike's lopsided rear cover and add a materials/texture pass), on `main`, with the same enormous working-tree changes already present at session start (confirmed untouched, not mine).
**Did:**
- Daniel reported the live Worker deploy as slow, stuttering, and going black for long stretches when turning. Confirmed via `npm run build` that the live bundle hash (`index-BiMSlBAa.js`) matches a local build of the current dirty tree exactly, so this is not a stale-deploy problem — the bug is in the current code.
- Root-caused one concrete bug in `src/camera/ThirdPersonCamera.ts`: `placeClearOfObstacles()` can shrink `boomFraction` to 0 when a wall stands between the orbit pivot and the desired camera position (routine near shopfronts/shutters, which is the core delivery loop). At `boomFraction === 0`, `camera.position` lands exactly on `pivot`, which is numerically identical to `lookTarget` in the default (non-riding) case — both are `anchor + (0, ORBIT_PIVOT_HEIGHT, 0)`. `camera.lookAt()` with a zero-length direction produces a degenerate view matrix, i.e. a black frame that persists for as long as that orientation (facing/near the wall) is held. Fixed by adding `MINIMUM_BOOM_DISTANCE` (0.6 m) and clamping the lerp fraction used for the actual camera placement so the lens can never coincide with the look target. `npx tsc --noEmit` and `npm test` (18/18) both pass after the change.
- While reproducing in the browser (dev build, `?view=nice-things&overlays=on`, orbiting via mouse drag next to the shopfront), also hit a *second*, separate black screen accompanied by hundreds of `GL_INVALID_FRAMEBUFFER_OPERATION: ... Attachment has zero size` console warnings, alongside frame times climbing past 900–1000 ms and draw calls/textures/geometries still rising well after the loading screen had already closed. Did not root-cause this one — dev mode loads the much heavier "development" asset set (355.8 MB vs production's 128.8 MB), so I can't yet tell how much of that is representative of the live production build versus a dev-only artifact. Wasn't able to reproduce it on the actual live Worker URL in this session because the automation's synthetic key presses don't register as held movement for `InputController`, so the player never actually walked into a wall there to test it.
- Follow-up: re-ran the same reproduction (`?view=nice-things&overlays=on`, repeated mouse-drag orbiting hard against the shopfront wall, both directions, at `quality=low`/`medium`) after switching `scripts/prepareRuntimeAssets.mjs` back to `--production` (128.8 MB, what's actually deployed) instead of `--development` (355.8 MB). No `GL_INVALID_FRAMEBUFFER_OPERATION` warnings, no black screen, and no runaway texture/geometry growth across many repeated close-range orbits — draw calls/textures/geometries rose modestly once (new shader compiles: Programs 112→115) and then held steady. The lookAt-collapse scenario that used to go black (camera pulled tight against the wall) now renders correctly. So the second issue was **not a production bug** — it was this session's own testing artifact from accidentally running the reproduction against the oversized dev asset set, which is never what a real player loads. Confirmed clean at both LOW and MEDIUM quality profiles.
**Left uncommitted (if any):** `src/camera/ThirdPersonCamera.ts` (the fix) plus this entry. Restored `config/runtime-assets.json` to the `--production` manifest before finishing (it was in that state at session start). Left the pre-existing giant uncommitted tree exactly as found.
**Flagged:** Nothing further open on this report. If Daniel still sees stutter/black-screen live after this lands, it's worth capturing the browser console during the actual repro, since a real `GL_INVALID_FRAMEBUFFER_OPERATION` there (as opposed to under oversized dev assets) would be a genuinely different bug than anything investigated in this session.
**Next:** None — consider this report closed pending Daniel confirming on the live deploy once `src/camera/ThirdPersonCamera.ts` ships.
**Open questions:** None.

---
## 2026-09-23 — Claude (Harperhey→Harpurhey rename; game Worker deployed to Cloudflare)
**HEAD at session start:** `6692d64` (Fix the Sterling Bike's lopsided rear cover and add a materials/texture pass), on `main`.
**Did:**
- Resolved the naming question every prior entry in this log left open (see "2026-09-11 — Claude (2)" and the Codex entry below it). Daniel's decision this time is final and explicit: **"Harpurhey" is correct everywhere; the earlier "Harperhey" fictional-spelling decision was a typo/error, not a deliberate distinction — do not treat it as intentional.** This reverses the "Claude (2)" 2026-09-11 decision.
- Renamed `harperhey` → `harpurhey` (case-preserved) across the entire repo: 73 tracked text files (docs, scripts, `package.json`, `package-lock.json`, `config/*.json`, `src/`, `index.html`), plus embedded strings inside 6 GLBs (glTF JSON `extras`/name fields — same byte length, so no structural risk; confirmed nothing in `src/` reads those fields by name) and 6 PNG renders (`tEXt` chunks — recomputed CRC32 after editing so the files stay spec-valid, verified with `file`/`sips`). Renamed 33 tracked files/paths (`git mv`) and one untracked `.blend1`. Verified with `npm run check` (18/18 tests, asset validation, rights check, `tsc && vite build`) — all green. Node 20.1.0 (the shell default) can't run this project's `tsc`/`vite` at all (unrelated pre-existing issue, not caused by this change) — use `nvm use 22` first, matching `ci.yml`'s pinned Node 22.
- Set up Cloudflare for the game. Checked the account via the Cloudflare MCP plugin (live API access, account `Danieloyegadephoto@gmail.com's Account`) and found significant Cloudflare work already in progress outside this session/repo: the `danieloye.com` zone is active on Cloudflare (nameservers cut over today), and its apex domain is **already live** on a new Worker called `daniel-oyegade` (confirmed via `curl`, serving the Astro site, `server: cloudflare`) — `www.danieloye.com` still CNAMEs to GitHub Pages and needs the same treatment. An R2 bucket `zealot-of-harpurhey-media` already exists with this game's model assets uploaded, correctly spelled — that was the tell that confirmed "Harpurhey" was the right call before Daniel even confirmed it in chat. There's also an unrelated Worker called `wxvei` (from March, no domain attached) — left untouched, flagged for Daniel to identify.
- Added `wrangler.jsonc` to this repo (Workers + Static Assets, no `main` — pure static, per Cloudflare's current recommendation over Pages). Found and fixed a real bug this surfaced: `vite.config.ts` had `base: '/zealot-of-harpurhey/'`, a leftover from an old plan to serve the game as a literal subfolder of the main site's file server. That's wrong for the architecture Daniel and I agreed on this session (game on its own Worker/origin, embedded into a `danieloye.com/zealot-of-harpurhey/` Astro page via `<iframe>`) — changed `base` to `'/'` and updated the stale README line describing the old assumption. Deployed with `wrangler deploy`: **live at `https://zealot-of-harpurhey.danieloyegadephoto.workers.dev/`**, verified in a fresh browser tab (screenshot: title screen renders correctly, zero console errors, all network requests 200).
**Left uncommitted (if any):** Everything above (rename across ~136 changed paths, `wrangler.jsonc`, `vite.config.ts`, `README.md`, `AGENTS.md`) — not committed, wasn't asked to. This working tree also still carries an unrelated in-progress bus-shelter texture pass and other asset work from a concurrent/prior session (`config/building-textures.json`, `blender/source/bus-shelter/*`, `docs/assets/bus-shelter.md`, `docs/assets/greek-gyros.md`, `renders/bus-shelter-texture-pass/*`, etc.) — left entirely untouched, not part of this entry's diff.
**Flagged:**
- `www.danieloye.com` still points at GitHub Pages (CNAME) — apex is migrated, www is not yet.
- Worker `wxvei` in the account is unexplained — not created by this session, no custom domain attached. Ask Daniel before touching it.
- The game Worker has no route/custom domain yet — it's only reachable at its own `workers.dev` URL. Wiring `danieloye.com/zealot-of-harpurhey/*` (Astro page shell + iframe pointing at this Worker) is the next step and hasn't been started.
- CI (`.github/workflows/ci.yml`) still only runs `npm run check`; no deploy step exists yet. Deliberately used `wrangler deploy` by hand this session rather than Cloudflare's git integration, because this repo's `.git` is ~2GB (committed binary assets) and git-connected builds would clone the whole thing on every push — a GitHub Actions step calling `wrangler-action` against the already-built `dist/` was recommended but not yet added.
**Next:** Add the Astro-side route (`danieloye.com/zealot-of-harpurhey/`) with the site's normal nav/chrome and an iframe pointing at `https://zealot-of-harpurhey.danieloyegadephoto.workers.dev/`; fix `www.danieloye.com`; add the GitHub Actions deploy step for this repo; decide whether to commit the rename batch.
**Open questions:** None on naming — considered closed. Still open: is `wxvei` safe to delete? Should the game eventually load its heavy assets from `zealot-of-harpurhey-media` (R2) instead of bundling them in the Worker's static assets, to avoid the ~129MB duplicate-storage/upload-per-deploy cost? Not evaluated this session.
